# Copy this file to .env.key and add your actual API key.
# Loaded by Spring as a .properties file — keep the value unquoted (no surrounding
# quotes; they would be read literally as part of the key).
ANTHROPIC_API_KEY=your-anthropic-api-key-here
