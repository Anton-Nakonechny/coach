# 06 REST and Security

API design conventions and the authentication/authorization mechanics behind a
Java backend's HTTP boundary.

- What is the difference between PUT and PATCH?
- What is JWT, and how does it work?
- How do you implement authentication and authorization in a REST API?
- What is CORS, and why does the browser enforce it?
- What is the difference between stateless and stateful authentication?
- You need to change an API without breaking existing clients — how would you design the new version?
