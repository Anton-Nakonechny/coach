# 03 Spring and Spring Boot

The container's lifecycle and wiring mechanics, and the Spring Boot conventions
that let a service start with almost no explicit configuration.

- Explain all phases of the Spring Bean lifecycle, up to BeanPostProcessor.
- How does Spring resolve circular dependencies between beans, and when does resolution fail?
- How does the Spring Security filter chain internally process a request?
- How do you use Spring Boot Actuator for production monitoring?
- Explain Dependency Injection, and why Spring is built around it.
- Constructor injection versus field injection — what's the practical difference?
- What is the difference between @Component, @Service, and @Repository?
- How does Spring Boot auto-configuration work under the hood?
- What does @Transactional actually do, and what are its semantics?
- How do you handle exceptions globally in a Spring Boot application?
- Why can @Transactional fail when a method is called from another method in the same class?
